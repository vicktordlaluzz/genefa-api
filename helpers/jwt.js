

const aleatorioEntero = (min, max) => {



};

const aleatorioDecimal = (min, max) => {
    const aleatorio = Math.random() * (max - min) + min;
    return aleatorio;
}

module.exports = {
    generarJWT
}